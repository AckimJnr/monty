#include "monty.h"
/**
 * nop - this operation doesn't do anything
 * @stack: the stack
 * @line_number: line being read
 *
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
